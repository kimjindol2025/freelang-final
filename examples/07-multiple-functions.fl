// 다중 함수 호출 (중급: 함수 조합)
fn add(a, b) {
  return a + b
}

fn multiply(a, b) {
  return a * b
}

fn compute(x, y, z) {
  result = add(x, y)
  result = multiply(result, z)
  return result
}

return compute(2, 3, 4)
